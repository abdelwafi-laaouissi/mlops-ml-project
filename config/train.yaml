data:
  kind: "iris"  # "iris" (baseline) ou "csv" (extension)
  path: "data/dataset.csv"
  target: "target"

split:
  test_size: 0.2
  random_state: 42

model:
  name: "random_forest"
  n_estimators: 100

artifacts_dir: "artifacts"